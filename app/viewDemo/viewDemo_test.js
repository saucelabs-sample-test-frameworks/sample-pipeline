'use strict';

describe('myApp.viewDemo module', function() {

  beforeEach(module('myApp.viewDemo'));

  describe('viewDemo controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewDemoCtrl = $controller('viewDemoCtrl');
      expect(viewDemoCtrl).toBeDefined();
    }));

  });
});
